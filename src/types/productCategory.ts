export interface productCategory {
  productImg: {
    mobile: string
    tablet: string
    desktop: string
  }
  title: 'New product' | null
  heading: string
  description: string
  btn: {
    btnText: string
    btnPath: string
  }
}
